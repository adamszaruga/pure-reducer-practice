const initialState = {
    id: 123
}

const reducer = (state, action) => {
    const { type } = action;
    if (type === "ADD_A_KEY") {

    } else if (type === "CHANGE_A_KEY") {

    } else if (type === "DELETE_A_KEY") {
        
    }

    return state;
}